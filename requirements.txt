# Основные зависимости для бэк-энда:
fastapi==0.95.0              # FastAPI для создания API
uvicorn==0.18.3              # ASGI сервер для запуска FastAPI
sqlalchemy==2.0.13           # SQLAlchemy для работы с базой данных
databases==0.6.0             # Асинхронная работа с БД (если требуется)
psycopg2==2.9.5              # PostgreSQL драйвер (если используется PostgreSQL)
pydantic==1.10.5             # Валидация данных с помощью Pydantic
python-dotenv==0.21.0        # Для работы с переменными окружения (например, для конфигурирования базы данных)
alembic==1.10.1              # Для миграций базы данных
fastapi-users==11.0.0        # Для работы с аутентификацией и пользователями
httpx==0.24.0                # Для выполнения асинхронных HTTP-запросов (если необходимо)
passlib[bcrypt]==1.7.4       # Для хэширования паролей

# Дополнительные зависимости для фронтенда:
# (Если вы используете какой-то backend-frontend фреймворк, например, через шаблоны)
jinja2==3.1.2                # Если нужно использовать Jinja2 для шаблонов

# Для работы с CORS (если ваш фронт на JS работает с другим сервером)
starlette==0.26.1            # Для поддержки CORS в FastAPI

# Для выполнения миграций с помощью Alembic
alembic==1.10.1

# Зависимости для работы с фронтендом (JavaScript)
# Эти зависимости будут добавлены в вашем проекте фронтенда на JavaScript. Пример:
# Для фронтенда на JavaScript:
# - Например, если вы используете Vue.js или React:

# Для фронтенда на React (если используете React):
# react@18.0.0
# react-dom@18.0.0
# axios (для HTTP-запросов с фронта к серверу)

# Для фронтенда на Vue.js (если используете Vue):
# vue@3.x
# vue-router@4.x
# axios

# Другие зависимости для проекта могут быть добавлены по мере необходимости.